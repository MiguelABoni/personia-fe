import { theme } from '../../theme/theme';

export const SX_GLOBAL = {
  CENTER_TEXT: { color: theme.palette.black.main, textAlign: 'center' },
  JUSTIFY_TEXT: {
    color: theme.palette.black.main,
    textAlign: 'justify',
  },
};
