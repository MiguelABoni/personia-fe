import { theme } from '../../theme/theme';

export const SX_TITLE_FLAG = {
  CONTAINER: {
    minWidth: '250px',
    height: '60px',
    padding: '1rem',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: '10px',
    boxShadow: '0px 4px 4px 0px rgba(0, 0, 0, 0.25)',
    backgroundColor: theme.palette.blueLight.main,
  },
};
